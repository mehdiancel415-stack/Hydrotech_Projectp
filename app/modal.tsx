// Route modale du template Expo, non utilisée par HydroTech.
// Stub minimal pour ne pas tirer ThemedText/ThemedView/etc. dans le bundle.
import { View } from 'react-native';
export default function ModalScreen() {
  return <View />;
}
